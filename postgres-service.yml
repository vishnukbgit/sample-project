apiVersion: v1
kind: Service
metadata:
  name: postgres-db-service
  labels:
    app: postgres-db
spec:
  ports:
    - port : 5432
      protocol: TCP
      targetPort: 5432
  selector:
    app: postgres-db
